module;
#include <memory>
#include "Core/Core.hpp"

module EEngine.Rendering:Buffer;
//import :OpenGLBuffer;

namespace EEngine {
	/*BufferElement::BufferElement(
		ShaderData type,
		const std::string& name,
		bool normalized
	) : Name(name),
		Type(type),
		Size(ShaderDataSize(type)),
		Offset(0),
		ComponentCount(ShaderDataComponentCount(type)),
		Normalized(normalized) {

	}*/
} // EEngine